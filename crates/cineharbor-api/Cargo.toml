[package]
name = "cineharbor-api"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# 跨仓依赖：共用 `cineharbor-core::model::SearchResult`（ADR-0007）。
cineharbor-core = { path = "../../../cineharbor-core/crates/cineharbor-core" }
html-escape = "0.2"
regex = "1"
serde.workspace = true
serde_json.workspace = true
url.workspace = true